# JARVIS — Plan for 2026-06-19

Working branch: `claude/pensive-cerf-288287`. Implementation will run under **ultracode**.

---

## 0. Where we are right now (done this session, uncommitted in the worktree)

All typechecked, **242 tests green**, lint clean. Nothing committed or deployed yet — it all lives in the worktree.

**Sync-overhaul safety fixes (from the 18-agent audit):**
- `lib/apple-reminders/ingest.ts` — guard against the empty/under-count snapshot that would **wipe every mirrored reminder** (the critical data-loss path). + tests.
- `lib/reconciliation.ts` — `reconcileStaleSchedule` now **honors the pause flag**, so a paused user's dashboard load no longer auto-misses tasks or deletes calendar blocks. + tests.
- Deleted `~/.jarvis/com.jarvis.imessage-reader.plist.bak` (plaintext secret from the retired agent).

**Raycast read-accuracy fix (Phase 0 of the daemon work):**
- `scripts/raycast/push-notes.py` + `schemas/raycast.ts` + `lib/raycast/ingest.ts` — each Raycast line is tagged `authored: user|agent` by its leading status icon; JARVIS counts only **David's** lines and keeps Scheduler/JARVIS board lines as labeled context. Live proof: 15 board lines split out. + tests.

**Audit artifact:** `docs/decisions/sync-system-spec.md` (full sync map).

**Phase 0 diagnosis (the key unknown, settled):** Raycast notes DB is **WAL-mode**; edits are readable via a WAL-aware read within **~2–8s of typing, no quit/relaunch needed**. The quit dance is only for *writing*.

---

## 1. The big picture — what JARVIS work is in flight

Two tracks:

**Track A — Sync system overhaul** (audit done; spec at `docs/decisions/sync-system-spec.md`).
- Safety fixes: **DONE** (above), pending commit.
- Phase B (decisions pending, NOT today unless chosen): auto-miss trigger relocation, the inbound-calendar feedback-learning loop, sync-health observability (staleness chip / "source-failed" rail card), launchd on-wake catch-up for the flaky overnight slot.
- Gmail "dark" finding — **resolved** (David reconnected with the right scope).

**Track B — Raycast ↔ JARVIS daemon** (the always-on assistant's I/O + control channel). Decisions locked: own "JARVIS" note (exists), explicit Raycast-command trigger first (FSEvents-ambient later), push transport (outbound long-poll), shared Scheduler writer + lock, confirmation handshake (deletion = receipt).

---

## 2. Today's build (ultracode) — scope: through **P3** (locked)

### Step 0 — One branch
- Everything stays on `claude/pensive-cerf-288287`; **single PR at end of day** (David: one big branch). The safe fixes already in the worktree ride along.

### Step 1 — P1 scaffolding
- **Scheduler ignore/awareness doc** in the `Claude - Scheduler` repo (following its `memory/`/`references/` structure) — **REQUIRED (David)**, for diagnosability + good practice; shouldn't change Scheduler's behavior much. It must state: (a) Scheduler **ignores the "JARVIS" note**, and (b) the **JARVIS daemon shares Scheduler's writer + `.claude-note-board.lock`** (so a future reader of that repo isn't surprised to see another process on the DB/lock).
- Confirm/prepare the **"JARVIS" note** as the interactive surface (it already exists in the Raycast DB).

### Step 2 — P2 the daemon (the main event)
Local Mac daemon bridging the "JARVIS" note ↔ cloud JARVIS:
- **Capture (you → JARVIS):** a **"Send to JARVIS" Raycast command** triggers the daemon → WAL-aware read of the JARVIS note → diff vs last-sent → upload to a JARVIS capture endpoint. (No quit needed — Phase 0 proved it.)
- **Push (JARVIS → you):** daemon holds an **outbound long-poll** to a JARVIS command endpoint (Mac unaddressable from Vercel, so it reaches out) → sleeps until JARVIS has something → writes the note via the **shared Scheduler SQLCipher writer** (surgical, lock-held, icon-tagged so it's not re-ingested).
- **Confirmation handshake (checkbox ack — locked):** command → JARVIS writes a **checkbox** `- [ ] ⚠️ Confirm: <action>?` into the JARVIS note → **David ticks it `[x]`** → daemon sees the tick on the next Send (ambient later) → JARVIS acts → **surgically deletes both the confirm line and the original command line** (deletion = done; no ✅ clutter).
- **JARVIS endpoints:** capture-ingest; a command queue with a **compare-and-swap claim** (do NOT copy the Canvas extension's broken claim); the long-poll endpoint. All behind the existing operator secret.

### Step 3 — P3 brain + Secretary (stretch for today)
- Unify the assistant brain so one path serves **both** the JARVIS note and the in-app **Cmd+K** surface (text in → interpret → act → reply out).
- Fix the **Cmd+K Secretary**: wire iMessage-archive retrieval ("No iMessage thread was loaded" bug) + bump the Opus option to `claude-opus-4-8`.

---

## 3. Decisions — locked vs still open
**Locked today:** scope through P3 · checkbox ack · share Scheduler's writer (with the required awareness doc) · one big branch.

**Still open (settle as we go):**
- **Icon-classifier breadth:** known Scheduler icon set (current) vs any-leading-emoji.
- **Long-poll latency target** for the push channel (how snappy the confirm round-trip feels).

## 4. Explicitly NOT today (deferred)
- FSEvents ambient capture (explicit-first this build).
- Sync overhaul Phase B items (above) — need David's decisions first.

---

## Guardrails
- The daemon writes the **encrypted Raycast DB** and stands up a **launchd process** — build/test with David present; **do not deploy unsupervised**.
- Reuse Scheduler's proven writer + lock; never bump `openedAt`; honor its sync/schema guards.
